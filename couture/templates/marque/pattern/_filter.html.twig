<div class="row">
    {{ form_start(form) }}
    {{ form_row(form.brand) }}
    <button type="submit" class="btn btn-outline-primary btn-lg col-1"><i class="fas fa-search"></i></button>
    {{ form_end(form) }}
</div>