<div class="my-3 text-dark">
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="d-flex justify-content-center">
        <button type="submit" class="btn btn-outline-primary btn-lg">{{ bouton_label }}</button>
    </div>
    {{ form_end(form) }}
</div>