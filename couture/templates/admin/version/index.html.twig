{% extends 'admin/layout.html.twig' %}

{% block title %}Liste des versions{% endblock %}

{% block content %}
    <div class="row"><i class="fas fa-list-alt fa-3x mr-3"></i><h1>Liste des versions</h1></div>
    <div class="my-3">
        <table class="table table-hover table-striped">
            <thead class="thead-dark h5">
            <tr>
                <th class="align-middle">Nom</th>
                <th class="col-2 align-middle"></th>
            </tr>
            </thead>
            <tbody>
            {% for version in versions %}
                <tr>
                    <td class="align-middle font-weight-bold h5">{{ version.name }}</td>
                    <td class="align-middle">
{#                        <a href="{{ path('admin_style_edit', {'id': style.id}) }}" class="btn btn-outline-primary"><i class="fas fa-wrench"></i></a>#}
{#                        {% if style.canBeDeleted %}#}
{#                            <a href="{{ path('admin_style_delete', {'id': style.id }) }}"#}
{#                               onclick="return confirm('Supprimer ce style ?')"#}
{#                               class="btn btn-outline-danger"><i class="fa fa-trash"></i></a>#}
{#                        {% endif %}#}
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}