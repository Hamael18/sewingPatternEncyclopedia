{% extends 'bo_partials/layout.html.twig' %}

{% block title %}Liste des rôles{% endblock %}

{% block content %}
    <div class="row"><i class="fas fa-list-alt fa-3x mr-3"></i><h1>Liste des rôles</h1></div>

    <div class="my-3">
        <table class="table table-hover table-striped">
            <thead class="thead-dark h5">
            <tr>
                <th class="align-middle col-1">
                    <a href="{{ path('admin_role_add') }}" class="btn btn-success"><i class="fas fa-plus"></i></a>
                </th>
                <th class="align-middle">Rôle</th>
                <th class="col-4 align-middle"></th>
            </tr>
            </thead>
            <tbody>
            {% for role in roles %}
                <tr>
                    <td class="align-middle"></td>
                    <td class="align-middle">{{ role }}</td>
                    <td class="align-middle">
                        <a href="{{ path('admin_role_edit', {'id': role.id}) }}" class="btn btn-outline-primary"><i class="fas fa-wrench"></i></a>
                        <a href="{{ path('admin_role_delete', {'id': role.id }) }}"
                           onclick="return confirm('Supprimer ce rôle ?')"
                           class="btn btn-outline-danger"><i class="fa fa-trash"></i></a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
