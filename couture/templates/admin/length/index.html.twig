{% extends 'bo_partials/layout.html.twig' %}

{% block title %}Liste des types de longueur{% endblock %}

{% block content %}
    <div class="row"><i class="fas fa-list-alt fa-3x mr-3"></i><h1>Liste des types de longueur</h1></div>
    <div class="my-3">
        <table class="table table-hover table-striped">
            <thead class="thead-dark h5">
            <tr>
                <th class="align-middle col-1">
                    <a href="{{ path('admin_length_new') }}" class="btn btn-success"><i class="fas fa-plus"></i></a>
                </th>
                <th class="align-middle">Nom</th>
                <th class="align-middle">Version(s)</th>
                <th class="col-2 align-middle"></th>
            </tr>
            </thead>
            <tbody>
            {% for length in pagination.data %}
                <tr>
                    <td class="align-middle"></td>
                    <td class="align-middle font-weight-bold h5">{{ length }}</td>
                    <td class="align-middle">{{ length.versions | length }}</td>
                    <td class="align-middle">
                        <a href="{{ path('admin_length_edit', {'id': length.id}) }}" class="btn btn-outline-primary"><i class="fas fa-wrench"></i></a>
                        {% if length.canBeDeleted %}
                            <a href="{{ path('admin_length_delete', {'id': length.id }) }}"
                               onclick="return confirm('Supprimer ce type de longueur ?')"
                               class="btn btn-outline-danger"><i class="fa fa-trash"></i></a>
                        {% endif %}
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
        {{ pagination.display }}
    </div>
{% endblock %}
